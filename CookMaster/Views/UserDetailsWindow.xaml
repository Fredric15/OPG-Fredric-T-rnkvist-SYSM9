<Window x:Class="CookMaster.Views.UserDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="UserDetailsWindow" Height="400" Width="400">
    <StackPanel>
        <TextBlock Width="Auto" Margin="5,5,5,0"> 
            <Run Text="Username: "/>
            <Run Text="{Binding Source={StaticResource UserManager}, Path=CurrentUser.UserName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold" FontSize="16"/>
        </TextBlock>
        <TextBox Text="{Binding NewUsername, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="New Password:" Width="Auto" Margin="5,5,5,0"/>
        <TextBox Text="{Binding NewPwd,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Confirm new Password:" Width="Auto" Margin="5,5,5,0"/>
        <TextBox Text="{Binding ConfirmPwd ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Width="Auto" Margin="5,0,5,0">
            <Run Text="Country:"/>
            <Run Text="{Binding Source={StaticResource UserManager}, Path=CurrentUser.Country, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" FontWeight="Bold" FontSize="16"/>
        </TextBlock>
        <ComboBox ItemsSource="{Binding Countries, Mode=OneWay}" SelectedItem="{Binding NewCountry ,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Select security question:" Width="auto" Margin="5,5,5,0"/>
        <ComboBox ItemsSource="{Binding SecurityQ, Mode=OneWay}" SelectedItem="{Binding Source={StaticResource UserManager}, Path=CurrentUser.SecurityQuestion, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Input your answer:" Margin="5,5,5,0"/>
        <TextBox Text="{Binding Source={StaticResource UserManager}, Path=CurrentUser.SecurityAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>

        <StackPanel Orientation="Horizontal">
            <Button Content="Save" Width="50" Height="25" Command="{Binding SaveCommand}" Margin="5,5,5,0"/>
            <Button Content="Cancel" Width="50" Height="25" Command="{Binding CancelCommand}" Margin="5,5,5,0"/>
            <TextBlock Text="{Binding ErrorText, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="WrapWithOverflow" Foreground="Red" Width="270" Margin="5,5,5,0"/>
        
        </StackPanel>
    </StackPanel>
</Window>
