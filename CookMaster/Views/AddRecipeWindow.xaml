<Window x:Class="CookMaster.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="AddRecipeWindow" Height="450" Width="400">
    <StackPanel>
        <TextBlock Text="Title:" FontWeight="Bold" Width="Auto" Margin="5,5,5,0"/>
        <TextBox Text="{Binding InputTitle ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Ingredients:" FontWeight="Bold" Width="Auto" Margin="5,5,5,0"/>
        <TextBox Text="{Binding InputIngredients ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinHeight="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5,0,5,0"/>
        <TextBlock Text="Instructions:" FontWeight="Bold" Width="Auto" Margin="5,5,5,0"/>
        <TextBox Text="{Binding InputInstructions ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinHeight="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"  Margin="5,0,5,0"/>
        <TextBlock Text="Choose Category:" FontWeight="Bold" Width="Auto" Margin="5,5,5,0"/>
        <ComboBox ItemsSource="{Binding Category, Mode=TwoWay}" SelectedItem="{Binding SelectedCategory, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="{Binding ErrorText}" TextWrapping="Wrap" Foreground="Red" Width="auto" Margin="5,5,5,0"/>
        
        <StackPanel Orientation="Horizontal">
        <Button Content="Add recipe" Height="25" Width="75" Command="{Binding AddRecipeCommand}" Margin="5,5,5,0"/>
        <Button Content="Cancel" Height="25" Width="75" Command="{Binding CancelCommand}" Margin="5,5,5,0"/>
        </StackPanel>
        
        
    </StackPanel>
</Window>
