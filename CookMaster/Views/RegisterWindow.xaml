<Window x:Class="CookMaster.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views" xmlns:local1="clr-namespace:CookMaster.ViewModel"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="400" Width="400" ResizeMode="NoResize">
    <StackPanel>
        <TextBlock Text="Username:" Width="Auto" Margin="5,5,5,0"/>
        <TextBox Text="{Binding UserName ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Password:" Width="Auto" Margin="5,5,5,0"/>
        <PasswordBox Name="PwdBox" PasswordChanged="PwdBox_PasswordChanged" Margin="5,0,5,0"/>
        <TextBlock Text="Bekräfta Password:" Width="Auto" Margin="5,5,5,0"/>
        <PasswordBox Name="ConfirmPwd" PasswordChanged="PwdBox_PasswordChanged" Margin="5,0,5,0"/>
        <TextBlock Text="Välj land:" Width="Auto" Margin="5,5,5,0"/>
        <ComboBox ItemsSource="{Binding Countries, Mode=OneWay}" SelectedItem="{Binding SelectedCountry, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Välj säkerhetsfråga:" Width="auto" Margin="5,5,5,0"/>
        <ComboBox ItemsSource="{Binding SecurityQ}" SelectedItem="{Binding SelectedQ, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="Ange ditt svar:" Margin="5,5,5,0"/>
        <TextBox Text="{Binding SecurityAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,5,0"/>
        <TextBlock Text="{Binding ErrorText}" TextWrapping="Wrap" Foreground="Red" Width="auto" Margin="5,5,5,0"/>
        <StackPanel Orientation="Horizontal">
            <Button Content="Register" Height="25" Width="50" Command="{Binding NewUserCommand}" Margin="5,5,5,0"/>
            <Button Content="Cancel" Height="25" Width="50" Command="{Binding CancelCommand}" Margin="5,5,5,0"/>
        </StackPanel>
        
    </StackPanel>
</Window>
