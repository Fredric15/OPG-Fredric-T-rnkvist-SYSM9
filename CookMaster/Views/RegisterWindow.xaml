<Window x:Class="CookMaster.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views" xmlns:local1="clr-namespace:CookMaster.ViewModel"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="400" Width="400" ResizeMode="NoResize">
    <StackPanel>
        <TextBlock Text="Username:" Width="Auto"/>
        <TextBox Text="{Binding UserName ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Password:" Width="Auto"/>
        <TextBox Text="{Binding Password ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Bekräfta Password:" Width="Auto"/>
        <TextBox Text="{Binding ConfirmPw ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Välj land:" Width="Auto"/>
        <ComboBox ItemsSource="{Binding Countries, Mode=OneWay}" SelectedItem="{Binding SelectedCountry, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="Välj säkerhetsfråga:" Width="auto"/>
        <ComboBox ItemsSource="{Binding SecurityQ}" SelectedItem="{Binding SelectedQ, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Text="{Binding SecurityAnswer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Content="Registrera ny användare" Command="{Binding NewUserCommand}"/>
        <TextBlock Text="{Binding ErrorText}" TextWrapping="Wrap" Foreground="Red" Width="auto"/>
    </StackPanel>
</Window>
