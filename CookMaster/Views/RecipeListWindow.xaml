<Window x:Class="CookMaster.Views.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        
        Title="RecipeListWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Content="{Binding Source={StaticResource UserManager}, Path=CurrentUser.UserName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" Command="{Binding UserDetailsCommand}" FontWeight="Bold" Background="Transparent" Height="30" Width="75" Margin="5" HorizontalAlignment="Left"/>
        <Button Content="Sign out" FontWeight="Bold" Command="{Binding SignOutCommand}" HorizontalAlignment="Right" Width="75" Height="30" Margin="5" />
        
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button
                Width="70"
                Height="30"
                Content="Add"
                Command="{Binding AddCommand}"/>
            <Button
                Width="70"
                Height="30"
                Content="Delete"
                Command="{Binding RemoveCommand}"/>
            <Button
                Width="70"
                Height="30"
                Content="Details"
                Command="{Binding DetailsCommand}"/>
            <Button 
                Content="Info"
                Width="70"
                Height="30"
                Command="{Binding InfoCommand}"/>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>


            <DataGrid
                IsReadOnly="True"
                AutoGenerateColumns="False"
                CanUserAddRows="False"
                ItemsSource="{Binding FilterRecipeList}"
                SelectedItem="{Binding SelectedRecipe}">


                <DataGrid.Columns>
                    <DataGridTextColumn Width="4*" Header="Titel" Binding="{Binding Title}" />
                    <DataGridTextColumn Width="4*" Header="Created By:" Binding="{Binding CreatedBy.UserName, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn Width="1*" Header="Category" Binding="{Binding Category}"/>
                </DataGrid.Columns>

            </DataGrid>

            <StackPanel Grid.Column="1">
                <Label Content="Filter by Category:" />
                <ComboBox ItemsSource="{Binding SelectedItem.Category, UpdateSourceTrigger=PropertyChanged}"/>
                <Label Content="Sort by date:" />
                <ComboBox ItemsSource="{Binding SelectedItem.Category, UpdateSourceTrigger=PropertyChanged}"/>                
            </StackPanel>

        </Grid>
    </Grid>
</Window>
