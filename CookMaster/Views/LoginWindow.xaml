<Window x:Class="CookMaster.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views" xmlns:local1="clr-namespace:CookMaster.ViewModel"
        mc:Ignorable="d"
        Title="CookMaster" Height="300" Width="530">
    <Window.DataContext>
        <local1:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"><Image Source="{Binding DisplayLogo}" Width="100" Height="100" Grid.Row="0" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/></TextBlock>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Username" FontSize="14" FontWeight="Bold" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5"/>
            <TextBox Width="75" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5"/>
            <TextBlock Text="Password" FontSize="14" FontWeight="Bold" Width="Auto" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5"/>
            <TextBox Width="75" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="5"/>
            <Button Content="Logga In" FontWeight="Bold" VerticalAlignment="Bottom" Margin="5"/>
            <Button Content="Registrera" FontWeight="Bold" VerticalAlignment="Bottom" Margin="5"/>
            <Button VerticalAlignment="Bottom" Width="55" Margin="5">
                <TextBlock Text="Glömt lösenord" FontWeight="Bold" TextWrapping="WrapWithOverflow"/>
            </Button>
        </StackPanel>
    </Grid>
</Window>
